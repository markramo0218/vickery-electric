document.addEventListener('DOMContentLoaded',()=>{const box=document.querySelector('.about-slideshow');if(!box)return;const slides=[...box.querySelectorAll('.slide')],dotsBox=box.querySelector('.slide-dots');let current=0,timer;const dots=slides.map((_,i)=>{const b=document.createElement('button');b.type='button';b.setAttribute('aria-label','Show project photo '+(i+1));b.addEventListener('click',()=>{show(i);restart()});dotsBox.appendChild(b);return b});function show(i){slides[current].classList.remove('active');dots[current]?.classList.remove('active');current=i;slides[current].classList.add('active');dots[current]?.classList.add('active')}function restart(){clearInterval(timer);timer=setInterval(()=>show((current+1)%slides.length),5000)}dots[0]?.classList.add('active');restart();box.addEventListener('mouseenter',()=>clearInterval(timer));box.addEventListener('mouseleave',restart)});